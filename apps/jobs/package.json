{
  "name": "@perf-tracker/jobs",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda handlers for background jobs (reminder sweep, AD sync). Phase 0 placeholder.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@perf-tracker/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
